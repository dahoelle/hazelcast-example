version: "3.8"

networks:
  hazelcast_network:
    driver: bridge
    name: hazelcast_network

services:
  # Used to display & visualize the individual Hazelcast clusters
  management-center:
    container_name: management-center
    image: hazelcast/management-center:latest
    ports:
      - 8080:8080
    environment:
      - MC_DEFAULT_CLUSTER=dev
      - MC_DEFAULT_CLUSTER_MEMBERS=217.154.206.223:4001
    networks:
      - hazelcast_network

  # The space-based virtualized middleware. Includes messaging-grid & deployment-manager
  middleware:
    container_name: middleware
    image: node:lts-alpine
    volumes:
      - ./middleware:/usr/src/app
      - ./hazelcast.yaml:/hazelcast.yaml # Hazelcast config is copied into new docker containers
      - /var/run/docker.sock:/var/run/docker.sock # Allows dockerode to access the Docker daemon
    command: node /usr/src/app/server.js
    ports:
      - 3000:4000
    environment:
      - HZ_PORT_START=4100 # Defines the port range of the cluster. Cluster #n has port 4100 + n
      - PU_PORT_START=3100 # Defines the port range of the PU. PU #n has port           3100 + n
      - PUBLIC_IP=217.154.206.223
      - MIDDLEWARE_NAME=middleware # Container name to pass to the instantiated PUs
      - MIDDLEWARE_PORT=4000 # Container port to pass to the instantiated PUs (internal docker port, not the exposed port)
    networks:
      - hazelcast_network

  #
  data-rw:
    container_name: data-rw
    image: node:lts-alpine
    volumes:
      - ./data-rw:/usr/src/app
    command: node /usr/src/app/server.js
    networks:
      - hazelcast_network

  # The space-based data-pump
  rabbitmq:
    container_name: rabbitmq
    image: rabbitmq:3-management
    ports:
      - 5672:5672
      - 15672:15672
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: admin
    volumes:
      - ./data-pump/lib:/var/lib/rabbitmq/
      - ./data-pump/log:/var/log/rabbitmq
    networks:
      - hazelcast_network

  # The space-based database
  mariadb:
    container_name: mariadb
    image: mariadb
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin
      MYSQL_DATABASE: space-based
    ports:
      - 3306:3306
    volumes:
      - ./database:/var/lib/mysql
    networks:
      - hazelcast_network

  #
  grafana:
    container_name: grafana
    image: grafana/grafana
    restart: always
    volumes:
      - ./grafana:/var/lib/grafana
    ports:
      - 80:3000
    networks:
      - hazelcast_network
    user: "0"

  #
  elasticsearch:
    container_name: elasticsearch
    image: elasticsearch:7.17.10
    restart: always
    volumes:
      - ./elasticsearch:/var/lib/elasticsearch/data
    environment:
      - discovery.type=single-node
      - http.port=9200
      - http.cors.enabled=true
      - http.cors.allow-origin=http://localhost:1358,http://127.0.0.1:1358
      - http.cors.allow-headers=X-Requested-With,X-Auth-Token,Content-Type,Content-Length,Authorization
      - http.cors.allow-credentials=true
      - bootstrap.memory_lock=true
      - "ES_JAVA_OPTS=-Xms2g -Xmx2g"
    ports:
      - "9200:9200"
      - "9300:9300"
    networks:
      - hazelcast_network
    user: "0"
